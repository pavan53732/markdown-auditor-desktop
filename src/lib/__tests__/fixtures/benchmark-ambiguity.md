# Scaling Limits

## Performance
The system scales very well.
It can handle a lot of concurrent users before it slows down.
You might need to adjust some settings if things get too slow.
The latency is usually fine.
