# Setup Guide

## Requirements
You must use Node.js version 18 exactly. No other versions are supported.
You must also use Node.js version 20.

## Installation
Run `npm install` to setup the project.

## Configuration
Set the `DEBUG` environment variable to `true` to enable verbose logging. 
By default, the system will not log any output regardless of environment variables.
