# Application Deployment

## Deployment Steps
1. Build the application using `npm run build`.
2. Start the server using `npm start`.

*Note: The server requires a running Redis instance and a valid JWT secret, but we don't tell you how to set those up.*
