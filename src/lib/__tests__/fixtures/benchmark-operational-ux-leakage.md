# Operational UX Leakage

Simple Mode promises calm messaging, but the UI shows raw stack traces, compiler logs, and internal retry counters.

The preview pane is a static mock while the product claims it is the real executable surface.

Users are told to intervene when needed, but no button or safe recovery action is described.
