# Vocabulary Drift Example

The specification uses `mission`, `task`, `job`, and `work item` as if they are interchangeable.

Later sections say the term `phase` is forbidden, but execution rules still rely on `phase` transitions.

No glossary defines which label is canonical, and the approval contract never maps the vocabulary to acceptance criteria.
